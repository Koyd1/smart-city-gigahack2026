[project]
name = "civis-backend"
version = "0.1.0"
description = "CIVIS municipal services assistant backend"
readme = "README.md"
requires-python = ">=3.12"

[tool.alembic]
script_location = "alembic"

[tool.pytest.ini_options]
pythonpath = ["."]
